[tool.poetry]
name = "matrix-loading"
version = "0.1.0"
description = "Matrix dependency loader and data visualizer"
authors = ["Camilla <camilla@student.42.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.1.0"
numpy = "^1.25.0"
matplotlib = "^3.7.2"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"